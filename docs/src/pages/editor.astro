---
/**
 * Full-width editor page — outside Starlight layout.
 *
 * Renders a bare HTML document with the Editor React component occupying
 * the full viewport. No sidebar, no TOC, no Starlight CSS interference.
 *
 * Monaco and esbuild-wasm are loaded client-side only.
 */
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Stoma Editor</title>
    <meta name="description" content="Write and test Stoma gateway configurations with live IntelliSense." />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png" />
    <style>
      /* Reset for full-viewport editor */
      *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
      html, body { height: 100%; overflow: hidden; background: #1e1e1e; }
    </style>
  </head>
  <body>
    <Editor client:only="react" />
  </body>
</html>
