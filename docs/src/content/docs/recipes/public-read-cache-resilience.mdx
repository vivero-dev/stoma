---
title: Public Read API (Cache + Resilience)
draft: false
sidebar:
  order: 4
---

import { Code } from 'astro:components';
import EditorLink from '../../../components/EditorLink.astro';
import cacheResilience from '@examples/cache-resilience/gateway.ts?raw';

## Use Case

Serve high-volume public GET endpoints with fast cached responses and graceful degradation when upstreams fail.

## Recipe

<Code code={cacheResilience} lang="ts" title="examples/cache-resilience/gateway.ts" />
<EditorLink code={cacheResilience} title="Public Read API (Cache + Resilience)" />

## Why it works

- Caching handles bursts and lowers origin load.
- Retry helps transient failures.
- Circuit breaker prevents cascading incidents.
- Timeout prevents hung upstream calls.
